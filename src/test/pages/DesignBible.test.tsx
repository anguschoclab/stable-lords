import { describe, it } from 'vitest';

describe.skip("DesignBible - file not found", () => {
  it.skip("renders without crashing - file doesn't exist", () => {
    // DesignBible.tsx doesn't exist, skipping this test
  });
});
